import { StyleSheet } from "react-native";
import { horizontalScale } from "../../util/scaling";

const styles = StyleSheet.create({
  container: { flex: 1, paddingHorizontal: horizontalScale(14) },
});

export default styles;
